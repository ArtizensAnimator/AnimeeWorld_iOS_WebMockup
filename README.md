# AnimeeWorld iOS Web Mockup

Static HTML mockup for the AnimeeWorld iOS experience.

## Run locally

Serve the repository root with any static HTTP server, for example:

```sh
python3 -m http.server 8080
```

Then open <http://localhost:8080>.

