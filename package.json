{
  "name": "animee-world-web-mockup",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Browser-runnable AnimeeWorld mockup shared between macOS and Windows",
  "scripts": {
    "check": "node --check scripts/serve-web.mjs && node --check backgroundTiles.js && node --check gameCode.js && node scripts/check-background-tiles.mjs",
    "dev": "node scripts/serve-web.mjs",
    "start": "npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
